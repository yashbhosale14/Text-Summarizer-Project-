name: workflow

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'

jobs:
  integration:
    name: Continuous Integration
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run basic CI validation
        run: |
          python - <<EOF
          from textSummarizer.pipeline.prediction import PredictionPipeline
          print("CI PASSED")
          EOF

  build-and-push-ecr-image:
    name: Continuous Delivery
    needs: integration
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 🔹 MOCK AWS AUTH (no real secrets needed)
      - name: Mock AWS credentials
        run: |
          echo "AWS credentials configured (mock)"

      # 🔹 MOCK DOCKER BUILD
      - name: Mock Docker build
        run: |
          echo "Docker build simulated"
          echo "Image built: text-summarizer:latest"

      # 🔹 MOCK DOCKER PUSH
      - name: Mock Docker push to ECR
        run: |
          echo "Docker push simulated"
          echo "CD PASSED"

  Continuous-Deployment:
    name: Continuous Deployment
    needs: build-and-push-ecr-image
    runs-on: ubuntu-latest

    steps:
      - name: Mock deployment
        run: |
          echo "Deployment simulated"
          echo "Application would be available on EC2"
